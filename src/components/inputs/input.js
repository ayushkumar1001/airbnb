'use client';
import React from 'react';

const Input = ({
  id = '',
  label = '',
  type = 'text',
  disabled = false,
  formatPrice = false,
  required = false,
  register,
  errors,
}) => {
  return <div>Input</div>;
};

export default Input;
